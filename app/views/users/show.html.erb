<h2 class="text-center mb-4"><%= @user.username %> Profile</h2>

<%= gravatar_for @user, size: 200 %>

<div class="container mt-4">
  <h4 class="text-center mb-4">Articles</h4>

  <%= render 'shared/articles_listing', articles: @user.articles %>
</div>
